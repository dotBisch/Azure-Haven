<!DOCTYPE html>
<html lang="en">
<head>
    @include('home.css')
</head>
<body>
    <!-- Header -->
    @include('home.header')

    <!-- Hero Section -->
    @include('home.hero')

    <!-- Booking Section -->
    @include('home.booking')

    <!-- About Section -->
    @include('home.about')

    <!-- Facilities Section -->
    @include('home.facilities')

    <!-- Footer -->
    @include('home.footer')
</body>
</html>